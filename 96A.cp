#include<bits/stdc++.h>
using namespace std;
int main()
{
ios_base::sync_with_stdio(false);
cin.tie(0);
string s;
cin>>s;
string s1="0000000"
string s2="1111111"
size_t found = s.find(s1,s2);
    if (found != string::npos)
	cout<<YES;
	elsea
	cout<<NO;
	return 0;
	}