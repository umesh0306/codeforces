#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	string s;
	cin>>s;
	size_t found =s.find(a,e,i,o,u, 0,s.end() )
	if(found!=s::npos)
		cout<<found;
	remove(s.found());
	return 0;
}
